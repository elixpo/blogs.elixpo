export const runtime = 'edge';
export const dynamic = 'force-dynamic';

// The shared handler accepts either a browser session or a scoped v1 bearer
// token. Keeping one implementation preserves the one-click/one-charge ledger.
export { POST } from '../../../ai/image/route';
