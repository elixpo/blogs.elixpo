import ProfilePage from '../../src/pages/ProfilePage';

export const metadata = {
  title: 'Profile',
  description: 'Your LixBlogs profile — manage your bio, avatar, and published content.',
};

export default function Profile() {
  return <ProfilePage />;
}
