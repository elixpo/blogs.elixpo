export const metadata = {
  title: 'Sign In',
  description: 'Sign in or create your LixBlogs account to start reading, writing, and collaborating.',
  robots: { index: false },
};

export default function AuthLayout({ children }) {
  return children;
}
