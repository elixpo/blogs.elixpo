export const metadata = {
  title: 'Edit Blog',
  description: 'Edit your blog post on LixBlogs.',
  robots: { index: false },
};

export default function EditLayout({ children }) {
  return children;
}
