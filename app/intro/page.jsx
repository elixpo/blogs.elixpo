import IntroPage from '../../src/pages/IntroPage';

export const metadata = {
  title: 'Welcome',
  description: 'Set up your LixBlogs profile — choose a username and get started.',
};

export default function Intro() {
  return <IntroPage />;
}
