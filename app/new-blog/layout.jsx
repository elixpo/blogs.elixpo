export const metadata = {
  title: 'New Blog',
  description: 'Create a new blog post on LixBlogs with the AI-powered block editor.',
};

export default function NewBlogLayout({ children }) {
  return children;
}
