see now that we ave a positon in the blog editer WYSIWYG what i want is to have this as a package 9 which can be iused by anyone in the intet to buuld an insatnt editor and an active renderer 

so what  we will do is we will fris tidentify all the bloatwares and resolve them without breaking any of the functionality we will do this in a new branch 

then create a packages folder whwre we will create the package with all the core componets engines etc etc 

we have to make sur ethat it wcan be imported in react or next and can be used lag ree with very low ram usage 

all the client side features only (excluding mentions) 


we will pack all our features in a package, and use the smae package in our product, so that we can maintain that, lixblogs will use the package wthat we publish and we will have no code redundance then 